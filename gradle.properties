# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://docs.gradle.org/current/userguide/build_environment.html

# JVM arguments for daemon process - increased memory for better performance
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8

# Enable parallel builds for multi-module projects
org.gradle.parallel=true

# Enable Gradle build cache for faster incremental builds
org.gradle.caching=true

# Enable configuration cache (experimental but significant performance improvement)
org.gradle.configuration-cache=true

# Limit parallel workers to avoid memory issues
org.gradle.workers.max=4

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true

# Enable incremental Kotlin compilation
kotlin.incremental=true

# Enable incremental Android builds
android.incremental=true

# R8 incremental mode for faster release builds
android.enableR8.fullMode=true

# ============================================
# Version Management (for local development)
# CI builds will be overridden by ORG_GRADLE_PROJECT_* environment variables
# ============================================

# Local development version name (semantic versioning format)
VERSION_NAME=0.0.1

# Local development version code
VERSION_CODE=1
